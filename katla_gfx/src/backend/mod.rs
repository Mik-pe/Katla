// Backend abstraction scaffolding for Metal/Vulkan unification (Phase E).
// Items here define the unified GPU backend API. Dead code is expected
// until both backends fully consume these interfaces.
#[allow(dead_code)]
pub mod command;
#[allow(dead_code)]
pub mod resource;
#[allow(dead_code)]
pub mod traits;
