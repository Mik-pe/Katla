# Active Context

## Current Work

- **Session 2026-09-20 (continued): worktrees pruned + two more slices** (main went `159c82aa` → `98600cb6` → `fc4bd253` → `7f3b87ba` → `cbd7b0d5`):
  - **Worktrees `Katla-101` and `Katla-37` DELETED, plus 10 local branches.** Both worktrees sat on pre-squash leftovers of already-landed PRs (their tips `ffe211a3`/`09e8a1f5` duplicate `4a784180`/`b6f7b447`). Verified rather than assumed: `git diff --stat main <branch> -- . ':(exclude)memory-bank/*'` showed only main's LATER work, and decisive per-slice checks (grep main's tree for the branch's introduced symbols: `FrameToken`, `PreparedDraws`, `SyncPlan`, `MeshDescriptor`, `PipelineDescriptor::billboard`, fallible `create_texture`) confirmed all nine `fix/<n>-*` branches were landed. **`git cherry` and subject-grepping are NOT reliable here** — squash-merge rewrites the patch, so `cherry` reports `+`, and grep fails because the squash subject carries the PR number. Remote `agent/*` branches are from 2026-07-26 (115+ commits behind) and reference merge-PR numbers (#39–#68), so they were left alone.
  - **#130 non-editor build fix** (squash `7f3b87ba`): `cargo check -p katla_app --no-default-features` had been broken since 2026-05-22 by `df29cb46` removing the `vulkan`/`metal` feature flags without collapsing two now-unconditional copies of `on_viewport_texture_recreated` (E0592), and the same removal made macOS non-editor builds render every frame TWICE (sequential ex-`cfg(vulkan)` and ex-`cfg(metal && !vulkan)` blocks). No CI job covers the non-editor config, which is why it rotted; the config IS documented as required in `.factory/library/user-testing.md`.
  - **#131 #35 tile-memory eligibility** (PR #131 squash-merged as `cbd7b0d5`, CI green both platforms): new `TileMemoryEligibility` in `allocation_plan.rs` classifies each slot member from its typed `ImageAccess` list (Eligible / Exported / AccessedOutsideAttachment / PartialSubresourceCoverage / NeverWritten); `combine()` ranks verdicts so a slot's reason is deterministic, not declaration-order dependent. `ExecutionPlan` gained `live_image_accesses` so **culled passes cannot influence the verdict**. Surfaced in JSON/text/DOT + `summary.tile_memory_eligible_bytes`. `ImageSubresourceRange::covers_whole_resource()` added to `access.rs`.
  - GOTCHA: the first golden re-bless surfaced real drift (`target/render-graph-diagnostics/*.json` written by the #129 artifact hook) — that hook works. Re-bless with `KATLA_BLESS_GOLDENS=1 cargo test -p katla_gfx --lib render_graph::diagnostics`, then read the diff.
  - GOTCHA: `#[cfg(test)]`-only helpers trip CI clippy's `-D dead-code`; an accessor used only by tests is a clippy error, so tests should read the field directly.

- **Session 2026-09-20 (continued 2): #31 buffer dependencies (PR #132 squash-merged as `9fb1e37b`, CI green both platforms)** — the render graph now models buffer resources with byte-range-aware dependencies, from the same one analysis that serves images.
  - **RENAME (affects anything touching the render graph):** `ImageAccessMode` → `ResourceAccessMode`, `ImagePipelineStage` → `ResourceAccessStage`, `ImageUsage` → `ResourceAccessUsage` (both kinds use them). MECHANICAL RENAME GOTCHA: a naive `grep -rl`+replace also rewrote Vulkan's `vk::ImageUsageFlags` into `vk::ResourceAccessUsageFlags` in 6 files; always re-check native type names after a bulk rename.
  - `BufferByteRange` (half-open, `WHOLE`, `from(offset)` = unbounded-to-end at `size == u64::MAX` mirroring the image `count == u32::MAX` convention), `BufferUsage` (8 classes), `BufferAccess`, `NamedBufferAccess` in `access.rs`.
  - Compiler: private `AccessRange` trait (`overlaps_with`/`subtract_range`/`is_empty_range`) lets ONE `ResourceAccessState<R>` serve both; `apply_access` is the shared RAW/WAR/WAW rule. `analyze_dependencies` versions images and buffers in separate maps, so **the two axes never order each other** (pinned by a test — resource ids are shared, so this is where a false dependency would hide).
  - Declaring: `PassDesc.buffer_accesses`, `SimplePass::buffer_access(name, mode, usage, stage, range)`, `PassDesc::with_buffer_accesses`. Diagnostics: `buffer_accesses` per pass in JSON/text/DOT (`bytes 4096+256`, `bytes 512+unbounded`); shared `diagnostic_access_mode`/`diagnostic_access_stage` helpers replaced the duplicated mapping.
  - GOTCHA: every `InternalPassBuilder { .. }` literal needed the new field — 12 sites, and they use BOTH `image_accesses,` (shorthand) and `image_accesses: x,` forms, so a single-form regex misses half of them.
  - REMAINING #31 (noted in the PR): transient/imported buffer creation + per-backend handle resolution, compute passes declaring real buffer deps (replacing out-of-graph `deduce_transition_masks` callers), buffer ops in `SyncPlan` (sync states are image-shaped), removing the insertion-order fallback, buffer aliasing (unblocks the #35 item).
  - Validation: 474 gfx lib tests (+8 compiler buffer tests, +1 diagnostics export test), fmt/clippy CI-exact clean, macOS cross-check compiles, 100-frame headless 0 ERROR / 1 WARN (baseline).

- **Session 2026-09-20: #37 slice 4 — render-graph capture + CI artifacts** (PR #129 squash-merged as `98600cb6`, Linux + macOS 26 CI green, main at `98600cb6`): the two self-contained #37 DoD items that needed no new backend data. `AnyFrameGraph::diagnostics()` (backend-neutral accessor over both `FrameGraph<B>`; compiler is pure so it needs no device). App capture: `ApplicationBuilder::dump_render_graph_to_stdout/to_file` + `game` flags `--dump-render-graph` / `--dump-render-graph-file PATH` (both imply `max_frames(3)`), one **ungated** `Application::dump_render_graph_if_needed` in `frame_loop.rs` (NOT duplicated per editor cfg — the logic only touches `frame_graph`), called from the windowed frame loop and `run_headless`. CI: both jobs upload `target/render-graph-diagnostics/` via `actions/upload-artifact@v4` `if: failure()`; `bless_or_compare` now WRITES the actual export there before asserting (honors `CARGO_TARGET_DIR`, else `<manifest>/../target`), so a drifting golden ships the disagreeing artifact. Docs: new `docs/render_graph_capture.md` (formats, local capture, diffing, blessing, known gaps) indexed in `docs/README.md` + a section in `docs/ci.md`. Verified on real Intel Vulkan: dump is 58 lines, byte-identical across runs, file and stdout modes identical after adding the trailing newline to file output. REMAINING #37 (still needs backend data): encoder/queue boundary identity (#33), frame-slot ownership (#36), Metal argument-table/residency fields (#55), runtime encoder traces + compiled-vs-emitted comparison (#56).
- **Session 2026-09-19: three issues landed, no open PRs, working tree = memory bank only** (main at `4a784180`):
  - **#89 CLOSED** (PR #127, squash-merged as `0f442ca5`, Linux + macOS 26 CI green): frame-scoped rendering API. `GpuRenderer::acquire_frame` → `FrameAcquisition::{Ready(FrameToken), Unavailable, OutOfDate}`; every frame-local op (`set_frame_uniforms`, `execute_draw_calls`/`draw`, `upload_lights`, `upload_shadow_cascades`, `render`) takes the token and validates it against the renderer's open frame; `present` consumes it, `abort` abandons it, a failed render poisons it. DELETED: `begin_frame`/`end_frame`, `wait_for_frame`, and the untokened variants. Device suite `katla_gfx/tests/frame_scope.rs` (6 tests). Found+fixed a real headless regression mid-slice (surface-image transitions gated on a windowed swapchain dropped readback's `TRANSFER_SRC_OPTIMAL` transition).
  - **#101 CLOSED** (PR #128, squash-merged as `4a784180`, Linux + macOS 26 CI green): prepared frame draw data. `PreparedDraws<'a>` borrows a pass's frame-owned submissions (`Rc<DrawList>` via `Frame::submit`); per-pass `merge_draw_lists` and Metal's rebuilt upload list deleted; Metal pass traces carry prepared counts. Bench: 64×8 draws 395µs/430 allocs → 9.7µs/21 allocs.
  - **#37 slice 3** (PR #126, squash-merged as `b6f7b447`): diagnostics schema v9→v10 — allocation slots carry compatibility class/logical bytes/saved bytes, members in alias-chain order, DOT renders slots as physical `cylinder` nodes; goldens re-blessed. #37 REMAINS OPEN (encoder/queue boundaries #33, frame-slot ownership #36, Metal argument-table fields #55, runtime encoder traces #56, CI artifact upload, capture/compare docs).
  - **KEY GOTCHA — macOS `cfg` code IS checkable on this Linux box** (contradicts the older note below): `CC="clang --target=arm64-apple-macos11" cargo check -p katla_gfx --tests --target aarch64-apple-darwin --locked` compiles the entire Metal module AND the integration tests. `CXX="clang++ --target=arm64-apple-macos11"` too. It caught three macOS-only errors before CI (dropped `MetalRenderer::frame_uniforms` impl, untokened `execute_draw_calls` in `frame_render.rs`, `wait_for_device().expect(...)` on `()` in `tests/contract/harness.rs`). Run it BEFORE pushing any change touching `cfg(target_os = "macos")`. `katla_app` still cannot cross-check (mlua/ring C++ build scripts need a real Apple SDK).
  - **KEY GOTCHA — `git commit --amend` without `-a` silently drops unstaged edits.** Cost two extra macOS CI cycles (committed the fix, but only the index, so CI tested the old tree). Always `git add -A` (or `git commit -am`) before amending after edits.
  - PR-body heredocs with backticks execute as shell — write the body to a file and use `gh pr edit --body-file`.

- Issue #35 slice 1 landed (2026-09-11, PR #125 squash-merged as 57f3c3b9, both CI jobs green, main CI green post-merge; issue REMAINS OPEN — evidence + remaining list in the issue comment): the compiled allocation plan now DRIVES allocation. `RenderGraphBackend::create_transient_texture` → `create_transient_slot(&[GraphResourceDesc])`; `initialize_transient_textures` builds `TransientAllocationPlan` (was diagnostics-only) and routes groups. Vulkan: multi-member slots = `ALIAS` images at offset 0 of one `VkDeviceMemory` per frame slot (max member size; memory-type intersection; attachment-only slots prefer LAZILY_ALLOCATED, unsampled depth gains TRANSIENT_ATTACHMENT usage); slot memory is `Rc<VkSlotMemory>` shared by members (destruction-order safe). Metal loops standalone allocations over the same grouping (heap/memoryless = later slices). `set_transient_aliasing(false)` debug switch. TWO sync fixes found by the new device tests: undefined-source bootstrap transitions now use ALL_COMMANDS/MEMORY_WRITE sources (aliased memory carries the prior member's attachment store; GPU-assisted validation caught the WAW), and `ImageBarrier` gained the missing TRANSFER_SRC→COLOR_ATTACHMENT masks (picking readback restore silently used TOP_OF_PIPE/NONE). Diagnostics schema v8→v9: `transient_slots` in JSON/text exports. GOTCHA: the editor graph's 5 transients are all exported/pinned or incompatible → 5 standalone groups, zero aliasing savings there; aliasing shows in graphs with compatible disjoint transients (e.g. diagnostics golden shadow_atlas+hdr_color). GOTCHA: headless `render()` calls `step_frame()` BEFORE returning — capture the frame slot BEFORE render for post-frame readback (reading the wrong slot shows UNDEFINED-layout zeros and trips the barrier helper's unsupported-transition fallback). Validation: 455 lib tests (+3 grouping), `tests/transient_aliasing.rs` device tests prove shared storage + switch behavior validation-clean, contract 12/12 serial on real Intel, headless 100-frame 0 graphics errors / 1 WARN baseline. Remaining #35: Metal heap + memoryless selection, per-class heap pools, buffer aliasing (needs #31), cross-frame-slot allocation ownership, tile-memory eligibility + savings estimates in diagnostics.
- Issue #37 slice 2 landed (2026-09-11, PR #124 squash-merged as 0c1da394, both CI jobs green; issue REMAINS OPEN — evidence + remaining list in the issue comment): synchronization transitions are first-class diagnostics (schema v7 → v8). `RenderGraphDiagnosticTransition` carries the op's subresource range (producer∩consumer access intersection); transitions ordered by EXECUTION position in every export; text export gained a `synchronization transitions:` section (frame-start steady-state seeds, per-pass ops, frame-end contract ops, with producer/consumer/range/states/hazard/reason); DOT renders frame-boundary ops as dashed edges through `frame_start`/`frame_end` nodes (incl. the anchor-less contract-only `frame_start -> frame_end` case); pass-to-pass ops deliberately stay on the dependency edges (no double-render). Golden-snapshot tooling: `katla_gfx/tests/goldens/render_graph_diagnostics.{json,text,dot}` pin a canonical shadow→geometry→lighting→present chain (imported backbuffer contract, RAW hazards, aspect-fragment bootstraps, `imported_final`); bless with `KATLA_BLESS_GOLDENS=1 cargo test -p katla_gfx --lib render_graph::diagnostics`; goldens run in the existing `--lib` CI step (CI runs NO integration tests except `--test contract`, so new test files under tests/ are NOT CI-gated — put export-pinning tests in `#[cfg(test)]` lib modules). Validation: 452 lib tests, fmt/clippy CI-exact, workspace suite, headless 100-frame 0 ERROR/1 WARN; rebased onto #123 mid-flight and re-validated. Remaining #37: encoder/queue boundary on transitions (#33 remainder), physical allocation/aliasing views (#35 — allocation slots view LANDED in #125; alias-relationship rendering in DOT still open), frame-slot ownership (#36), Metal residency fields (#55), encoder traces + compiled-vs-emitted comparison (#56), CI artifact upload, capture/compare docs.
- Issue #88 CLOSED (2026-09-11, PR #123 squash-merged as ded9c091, both CI jobs green): material identity is separate from render-pass-specific pipeline variants. `PipelineVariantKey` (renderer/pipeline_variant.rs, backend-neutral Hash+Eq) = the material's `PipelineDescriptor` with color_format resolved concrete + shared `derive_depth_format` (None for UI/compositing/depth-test-disabled, else D32SfloatS8Uint — one derivation for BOTH backends now) + samples (const 1); `MaterialAsset` = {descriptor, variants: HashMap<Key, MaterialVariant{pipeline, instanced_pipeline}>, textures} — `fully_compiled`, single-pipeline fields, pinned `color_format`, and `MaterialOptions`/`MaterialType` deleted. Vulkan threads pass output format into execute_draw_list/resolve_draw_commands/UI/compositing (fallback unwrap_or(Auto) → key resolves to the material's DECLARED format, preserving old concrete-material behavior); Metal ensures variants in collect_draw_lists for PassKind::Geometry draw lists (same filter as Vulkan's pre_compile) + UI pass materials at drawable B8G8R8A8Srgb, encode resolves via `material_pipeline`. Hot reload drops matching materials' variants (recompile on next use); layout invalidation drops ALL variants (`invalidate_all_material_variants`). Typed `MaterialError::VariantCompilation{material, color_format, depth_format, reason}`. Deleted with the old model: write-only `material_descriptor_set/layout` fields + the unreachable standalone layout retirement chain (`RetiredDescriptorSetLayout`, `RetirementKind::DescriptorSetLayout`, snapshot field). Device suite `tests/pipeline_variants.rs` (6 tests incl. a validation-Enabled guard that deferred Auto materials never bind UNDEFINED-format pipelines — VUID-08963/08910). The 100-frame headless gate caught exactly that bug mid-development (21 errors → building variants from key.descriptor() not the raw descriptor fixed it back to 0 ERROR/1 WARN). Remaining #88 (see issue comment): MSAA sample wiring, specialization constants still rejected pre-cache, Metal post_process_later forced-HDR lookahead → #56.
- Issue #98 emission slice landed (2026-09-11, PR #122 squash-merged as a011ec7f, both CI jobs green; issue REMAINS OPEN — evidence + remaining list in the issue comment): `DrawCall.emission` and the app's `DrawableComponent.emission` are typed `TextureHandle` (default NONE); both backends resolve the handle at prepare/encode (Vulkan `resolve_emission_texture_slot`, pub in bindless_queries for validation; Metal `resolve_emission_texture_slot_impl`); `DrawCall::material_params` now takes the backend-resolved emission slot — the `material_params.w` slot ABI is unchanged (billboard's reuse of w as albedo index intact), no shader edits. NONE/stale handles resolve to 0 = the shaders' `emission_idx > 0u` no-emission sentinel, deliberately NOT `DEFAULT_EMISSION_SLOT` (0 skips the sample entirely). App GLTF loader and billboard path bind handles directly (app-local `get_bindless_index` helper deleted; write-only `EditorState::stencil_indicator_bindless_index` raw-index field deleted). Device suite `tests/typed_emission_binding.rs`. GOTCHA learned here: `cargo test --workspace --tests -- --ignored` HANGS — pre-existing ignored katla_app tests are traps (transform_hierarchy cycle-detection test spins forever at 100% CPU; ui editor_ui hit-testing) — device suites must be scoped `-p katla_gfx --tests -- --ignored`. Remaining #98: viewport transient TextureIds + `set_overlay_texture_indices` (needs graph-resource identity); `Texture::resize` replacement retirement (from #84).
- Issue #97 CLOSED (2026-09-11, PR #119 squash-merged as b0f42fc3, both CI jobs green incl. the suite itself): `katla_gfx/tests/contract/` — one cross-backend contract suite running the same scenarios on the platform backend (Vulkan on Linux/lavapipe in CI, Metal on macOS CI with MTL_DEBUG_LAYER + METAL_DEVICE_WRAPPER_TYPE — first real Metal device execution in CI). Backend-neutral harness (`ContractRenderer`: headless init, AnyFrameGraph compile, per-frame BGRA readback per backend, `Capabilities` table, finish() asserts the captured validation log); 12 scenarios in graphics/resources/errors modules. Found+fixed FOUR real divergences: (1) Metal compiled every non-UI pipeline RGBA16Float + unconditional depth, ignoring `PipelineDescriptor.color_format`/`DepthState` — now honored (Auto keeps the HDR default for the app tonemap chain); (2) Metal skipped the static-mesh-immutable rejection in update_mesh_dynamic (MetalMesh now records MeshUsage); (3) Metal's geometry/UI record handlers ignored graph-declared attachments — geometry binds depth only when `record.depth_attachment` is Some (the builder injects reverse_z_default into uses_depth passes → new `GeometryPass::without_depth()` for depth-free passes) and honors declared color ops; UI record renders the pass's DECLARED material (set_ui_material trait method + Metal field + app call removed); (4) Metal normalizes u16 indices to u32 (capability `preserves_index_width`). GOTCHAS: lavapipe segfaults in the PBR pipeline path → Linux CI runs `--skip graphics::pbr --test-threads=1` (serial: pipe-loader race); Metal renders direct-to-drawable graphs vertically mirrored vs Vulkan (the app's tonemap does the flip) → capability `flips_direct_ndc_y` flips probe mapping; UI pixel space is top-down on BOTH (use `ui_pixel_offset`); macOS-only cfg code (mut fields, test constructors) never compiles on Linux — two CI cycles burned on unused_mut + a missing MetalPassRecord field in a cfg(test) constructor.
- Issue #92 core slice landed (2026-09-11, PR #120 squash-merged as 47a66ff5, both CI jobs green; issue REMAINS OPEN — evidence + remaining list in the issue comment): katla_gfx's root API is backend-neutral. All implicit `pub use vulkan::...` re-exports removed; example-facing native types (ShaderCache, CommandBuffer, Pipeline/PipelineBuilder, VertexFormat, ComputePipeline/Builder) live behind feature-gated `katla_gfx::vulkan_native` (documented lifetime/safety commitments); root `FrameGraph` alias (Vulkan-selecting) deleted; `MaterialOptions`/`VertexType` crate-internal and the inherent `VulkanRenderer::compile_material(path, options)` demoted to `pub(crate)` — the only public material path is `GpuRenderer::compile_material(&PipelineDescriptor)`. `RetirementSnapshot` → `renderer::retirement`; `CompositingDescriptorSet` → `vulkan::compositing`. `CompareOp`/`CullMode`/`FrontFace`/`DepthState` now unconditionally public (they type PipelineDescriptor fields). App's five material sites migrated; gizmo/billboard no longer branch AnyRenderer::Vulkan vs Metal into different effective materials (Metal fallback branches dropped). New device suite `tests/backend_neutral_api.rs`. REMAINING #92: compute neutrality (katla_gfx::compute module + PoseCompute* vk-typed signatures → #32's graph-command replacement); app per-backend graph presets (`build_frame_graph`/`build_metal_frame_graph`) need generic-over-backend build (AnyRenderer is NOT a RenderGraphBackend) + Metal parity → #56.
- Issue #33 slice 1 landed (2026-09-11, PR #118 squash-merged as 922778f9, both CI jobs green, main CI green post-merge; issue REMAINS OPEN — evidence + remaining list in the issue comment): graph synchronization is compiled from the typed accesses. `ExecutionPlan.sync` (`render_graph/sync_plan.rs`) holds per-pass ordered image ops (range, before/after `ImageSyncState` = usage+stage+mode, hazard reason, import-contract initial/final); Vulkan realizes them as sync2 barriers with explicit masks/layouts/aspect-intersected ranges (no layout-pair inference, no TOP_OF_PIPE fallback on the graph path). DELETED: PassBarrierCache + ensure_barrier_cache (O(n²) rescan), insert_post_pass_barriers, shadow-pass state poke, dead trait transition hooks, `ResourceState::to_vk_*`, `TransientTextureOps` state Cells (both backends). Metal consumes the same plan via documented tracked-resource classification (`MetalSyncCoverage`, unit-tested). Diagnostics schema v7. KEY MODEL: frame-periodic steady state (discovery scan seeds transients from prev-frame end states) + discard-safe Undefined→target bootstrap ops for fresh textures that realization coalesces once the tracked layout matches — an earlier iteration of the branch rendered single-write transients (object-id, stencil indicator) from UNDEFINED layout on frame 0 and the 100-frame headless run caught it as VUID-09592 x10; that run is the only gate that exercises the full editor graph. REMAINING #33: backbuffer contract consumption (renderer acquire/present barriers stay), out-of-graph barrier callers keep `deduce_transition_masks` until #31, scene depth keeps `depth_render_pass_sync` (backend-owned), queue/encoder boundary modeling.
- Issue #30 CLOSED (2026-09-11, PR #116 squash-merged as 09573dcf, both CI jobs green, main CI green post-merge): typed image accesses are now hand-declared everywhere and imported images carry state contracts. **This also fixed a render-blocking regression from slice 1 (c3500878, landed 2026-09-10): the shadow pass was CULLED on every editor graph** — its inferred write refined to a DEPTH-only range while `GeometryPass.read("shadow_atlas")` inferred a COLOR-only `sampled_read`; disjoint aspects → no hazard edge → liveness dropped the pass → every frame errored `Pass 'shadow' was culled` → black screen (17 KB capture vs healthy 586 KB). Neither CI nor device suites exercise the editor graph; only the game binary exposed it. The "pre-existing" interaction-harness `viewport_click_picks_object` failure (7/8) recorded on 2026-09-11 was THIS regression — harness is 8/8 after the fix. Fix: generic `sampled_read` declares the whole-resource range (ALL aspects; the graph cannot narrow sampling without the image format); `.with_range` for precision. New: `ImportedImageContract {initial, required_final}` (`undefined()`/`arrives_in()`/`.must_end_in()`), `import_resource(name, handle, contract)`, `backbuffer_contract(...)` (default = observable contents, replacing the name-based load exemption); compile validation (`LoadingUndefinedImportedContents`, `UnreachableImportedFinalState`); diagnostics carry contracts (schema v6). All 11 templates hand-declare `NamedImageAccess` via `named_image_access` (passes/mod.rs); per-template tests pin declarations; `refine_inferred_image_accesses` remains only for low-level `PassDesc`/`SimplePass`. GOTCHA for validation: the editor graph only compiles through the GAME BINARY (katla_app) — run a headless capture when touching template inference/liveness.
- Issue #98 core slice landed (2026-09-11, PR #117 squash-merged as 6da53d3c, both CI jobs green; issue REMAINS OPEN for follow-up slices — evidence + remaining list in the issue comment): material texture bindings are typed. `MaterialTextures` = `TextureHandle` per PBR role (albedo/normal/metallic_roughness/occlusion; default all-NONE); `set_material_textures(material, MaterialTextures)` replaces `set_material_texture_indices` across GpuRenderer/AnyRenderer/Vulkan/Metal/mock. Each backend resolves handles→slots at exactly one point (Vulkan `resolve_material_texture_slots` in execute_draw_calls — pub for binding-table validation; Metal `resolve_material_texture_slots_impl` at object upload); NONE and stale handles resolve to per-role `DEFAULT_*_SLOT` (now pub in katla_gfx::texture) — a stale handle can never sample a recycled slot (composes with #83/#84). App GLTF loader binds handles directly (index derivation deleted). Device suite `tests/typed_material_textures.rs` (per-role resolution, stale fallback ≠ destroyed slot, shared texture across materials). Remaining #98 slices: viewport transient TextureIds + `set_overlay_texture_indices` (needs graph-resource identity); `Texture::resize` replacement retirement (from #84) — emission typed by PR #122.
- Issue #98 UI slice landed (2026-09-11, PR #121 squash-merged as 44c64a37, both CI jobs green after one pipeline_archive macOS flake rerun; issue REMAINS OPEN — evidence + remaining list in the issue comment): `UiDrawCommand.texture` was write-only state and is deleted (Vulkan never consumed it; Metal pushed `.index()` into a `UiUniforms` member no shader entry point reads — UI texture selection is the per-instance/per-vertex `texture_index` ABI). App `UIRenderer` no longer fabricates `TextureHandle::from_raw(bindless_slot, 0)`. `UiUniforms` is one `vec4f` `[width, height, ndc_y_flip, unused]` — Metal's inline push now byte-identical to Vulkan's UI uniform buffer write. GOTCHA confirmed: `cargo test -- --ignored` fails katla_gfx DOCTESTS by design (the pre-existing `bda.rs` ` ```ignore ` fragment gets compiled and cannot) — run device suites as normal-mode workspace tests + a targeted `--ignored` pass, and expect that doctest artifact; `pipeline_archive::test_pipeline_archive_rebuilds_on_metadata_mismatch` (assert `opened_from_disk` after flush+reopen) is CI-flaky — rerun the failed job before debugging.
- Issue #84 fixed (2026-09-11, PR #115 squash-merged as 126312ce, issue closed; full detail in progress.md): generalized `RetirementQueue` — destroy_texture/destroy_material/destroy_skeleton, pipeline replacement (hot reload + layout invalidation), and UI auto-grow all defer native frees; bindless slots withheld until expiry; `pending_retirements()` snapshot diagnostics; device suite `tests/resource_retirement.rs`.

- Issue #37 slice 1 landed (PR #113; details in progress.md). #37 remains the open umbrella.
- PENDING: `katla_gfx/AGENTS.md` no-op-default guidance update is instruction-protected — needs Micke's explicit approval (proposed text ready, see PR #105 discussion).

- Metal visual verification pass (2026-09-05, macOS): the shared sky/UI shader changes from the Vulkan audit are now verified on native Metal. Two Metal-only regressions found and fixed: (1) the UI vertex descriptor lacked the `texture_index` attribute the shader now reads per-vertex, so the UI material failed to compile and EVERY frame errored ("Metal UI record has no material") with a nearly blank canvas; (2) cascaded shadow atlas content was vertically mirrored inside each atlas quadrant — Metal clip space is Y-up while the shared cascade data and sampler follow Vulkan's Y-down convention — which displaced/mirrored all sun shadows ("inverted shadows"). Fixed by a Metal-only encode-side cascade buffer with flipped clip-Y matrices plus `MTLWinding::CounterClockwise` on the shadow pipelines; sampling keeps the shared Vulkan-convention data. Verified headless from default/side/back/top-down angles plus the playground scene; red-shadow-mask shader probe (temporary, reverted) confirmed the mask tracks casters. Metal validation run (`METAL_DEVICE_WRAPPER_TYPE=1 katla -s`) exits clean.
- The game binary gained a `--camera yaw,pitch,distance` diagnostic flag (degrees) for headless captures from explicit orbit poses; `Application::set_editor_camera_pose` drives it.
- Pre-existing macOS-only clippy warnings fixed: `encode_cascade_draws` too-many-arguments allow, and the macOS `collect_and_upload_lights` now reuses `point_lights_buffer` instead of allocating per frame.

## Ongoing Architecture Work

- Complete render-graph execution plans (#56): graph-declared attachment/load/store/clear policy, viewport/scissor, and generic executable payloads still need to reach all native handlers. Exact pass identity, application-owned topology, pass-local submissions, explicit picking, and dead-pass culling already exist.
- Preserve the engine/application boundary: custom graphs may be empty, UI-only, reordered, or repeated. Never invent editor topology in a backend.
- Shadow and depth work remain explicit side-effect roots while their native targets are backend-owned. Vulkan particle emission/simulation, animation, and light-culling work also require explicit side effects until their buffers are graph resources.
- The Metal frame-uniform preparation bridge belongs in the eventual frame-slot/buffer ownership design (#36/#31). Some Metal handlers still resolve backend-owned textures; transient allocation is not live-range aliased.
- Metal particle reset and entity-destruction cleanup still need routing through the common emitter driver.
- Private Metal texture storage sampling still needs an Xcode GPU capture; the storage-mode probe is the starting point. Staged uploads and shared storage already work.

## Conventions and Validation Limits

- Object storage slots are allocated ONLY by `DrawList::push` (assigns a unique base range and returns it) — never set `instance_index` by hand. Filtered/merged/cloned draw lists must use `DrawList::from_draws` to preserve uploaded slots; re-pushing clones would reassign and desynchronize upload from encode.
- Reserve declarative editor state slots unconditionally in a stable order. Conditional slots cause cross-view type confusion. The inspector reserves one expansion slot per `SECTION_TYPES` entry plus the picker filter String slot, all before any conditional build.
- Component type names in `EntityInfo.components` must match `ComponentRegistry` type names exactly — the UI filters the add-picker by comparing them.
- Use UI design tokens for chrome dimensions. Docked content uses panel bodies; dock tab strips provide titles.
- Vulkan frame waits do not reset fences; reset only immediately before submission. Offscreen submissions complete before returning for deterministic readback ownership.
- Canonical Linux and macOS 26 CI are required before merging. This Linux session cannot *run* native Metal, but it CAN typecheck the Metal module and its integration tests: `CC="clang --target=arm64-apple-macos11" CXX="clang++ --target=arm64-apple-macos11" cargo check -p katla_gfx --tests --target aarch64-apple-darwin --locked`. Do this before pushing anything touching `cfg(target_os = "macos")` — it caught three macOS-only errors that would otherwise have cost three CI cycles. `katla_app` cannot be cross-checked (mlua/ring C++ build scripts need a real Apple SDK).
- Synthetic input in headless: inject in `InteractionTestRunner::begin_frame` (before the frame renders) via `ui_context.input_mut()` for UI clicks/scroll and `app.on_mouse_input` for viewport picks; presses and releases must land on separate frames. The interaction harness lives in `katla_app/src/application/interaction_test.rs`; run with `cargo run -- --interaction-test DIR`. Section-header × hit zones are the rightmost ~20px of the header; picker row coordinates shift when the scene changes section counts.
- GOTCHA: /tmp tmpfs quota can fail katla_app doctest linking ("LLVM ERROR: IO failure on output stream: Disk quota exceeded") — rerun with `TMPDIR=<home dir>`. `katla_audio::test_engine_playback_lifecycle` is flaky on this machine (stop/state timing race; passed in the 2026-09-08 run); pre-existing, unrelated to UI work.
- GOTCHA: run `cargo fmt --all -- --check` immediately before EVERY push — clippy fixups after formatting drift (PR #115's CI failed on this once).
- GOTCHA (GPU tests): rendering a graph with no submissions still validates pass materials — a destroyed graph material errors every later frame; use a separate live graph material + a victim material when testing destroy paths.
- GOTCHA (bindless): the free-slot list is a LIFO stack — after a slot is released, the NEXT texture registration pops it (resource_retirement + typed_material_textures device tests rely on this ordering).
