name: Agent validate render graph

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    runs-on: macos-14
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Smoke test validation harness
        run: echo "render graph validation harness is active"
