name: Agent Metal frame schedule

on:
  pull_request:
    branches:
      - agent/render-graph-compiler

permissions:
  contents: read

jobs:
  validate:
    runs-on: macos-14
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Smoke test validation harness
        run: echo "Metal frame schedule harness is active"
