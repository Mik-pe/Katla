{
  "groupId": "rg-patterns",
  "testedAt": "2026-04-08T12:00:00Z",
  "isolation": {
    "workingDirectory": "C:\\dev\\katla",
    "uncommittedChanges": true,
    "tool": "CLI (cargo check, rg)"
  },
  "toolsUsed": ["cargo check", "rg"],
  "assertions": [
    {
      "id": "VAL-ROBUST-001",
      "title": "Replace per-frame .expect() calls",
      "status": "pass",
      "steps": [
        {
          "action": "rg '\\.expect\\(' katla_gfx/src/renderer/ katla_gfx/src/render_graph/ --files-with-matches",
          "expected": "Any remaining .expect() must be init-time only",
          "observed": "8 .expect() calls found in renderer/; 0 in render_graph/. All renderer calls are in init-time functions: create_texture (texture_api.rs:30), init_resources (shadow.rs:338,343), build_outline_pipeline (outline.rs:273,279), new/ViewportBuilder (mod.rs:1266), default_material (material_api.rs:136)"
        },
        {
          "action": "cargo check -p katla_gfx",
          "expected": "Exit 0",
          "observed": "Exit 0 — compilation succeeded"
        },
        {
          "action": "Check render function signature in renderer/mod.rs",
          "expected": "Render loop returns Result",
          "observed": "pub fn render<F>(...) -> Result<(), crate::error::RendererError> at line 1064"
        }
      ],
      "evidence": {
        "expectCalls": "8 total in renderer/, 0 in render_graph/. All init-time: texture_api.rs:30 (create_texture), shadow.rs:338,343 (init_resources), outline.rs:273,279 (build_outline_pipeline), mod.rs:1266 (ViewportBuilder::new), material_api.rs:136 (default_material)",
        "renderGraphExpect": "0 matches — clean",
        "renderSignature": "pub fn render<F>(...) -> Result<(), RendererError>",
        "cargoCheck": "Exit 0"
      },
      "issues": null
    },
    {
      "id": "VAL-ROBUST-002",
      "title": "GpuAllocator wrapper replaces ManuallyDrop<RefCell<Allocator>>",
      "status": "pass",
      "steps": [
        {
          "action": "rg 'GpuAllocator' katla_gfx/src/",
          "expected": "GpuAllocator type defined and used throughout",
          "observed": "22 matches: GpuAllocator struct defined in vulkan/context/memory.rs:17, used in context/mod.rs:97 (pub allocator field), 4 implementations (alloc, free, alloc_string, free_string) in memory.rs:21-105"
        },
        {
          "action": "rg 'context\\.allocator\\.borrow_mut' katla_gfx/",
          "expected": "ZERO matches",
          "observed": "0 matches — clean"
        },
        {
          "action": "rg 'try_borrow_mut' katla_gfx/",
          "expected": "ZERO matches in production code outside wrapper",
          "observed": "15 matches total: 3 inside GpuAllocator wrapper (memory.rs:51,77,97), 12 are callers using try_borrow_mut_string (the wrapper's safe method). No raw RefCell::try_borrow_mut() calls outside the wrapper."
        },
        {
          "action": "rg 'allocator\\.borrow_mut|allocator\\.borrow\\(\\)' katla_gfx/",
          "expected": "ZERO matches",
          "observed": "0 matches — clean"
        },
        {
          "action": "cargo check -p katla_gfx",
          "expected": "Exit 0",
          "observed": "Exit 0 — compilation succeeded"
        }
      ],
      "evidence": {
        "gpuAllocatorDefined": "vulcan/context/memory.rs:17 — pub struct GpuAllocator",
        "contextAllocatorBorrowMut": "0 matches",
        "borrowMutOnAllocator": "0 matches",
        "tryBorrowMut": "15 matches: 3 in wrapper impl, 12 callers using try_borrow_mut_string (safe wrapper method)",
        "cargoCheck": "Exit 0"
      },
      "issues": null
    },
    {
      "id": "VAL-ROBUST-003",
      "title": "Guard asset browser edge cases",
      "status": "pass",
      "steps": [
        {
          "action": "rg '\\.unwrap\\(\\)' katla_app/src/ui/editor_ui/asset_browser/mod.rs",
          "expected": "ZERO matches",
          "observed": "0 matches — clean"
        },
        {
          "action": "cargo check -p katla_app",
          "expected": "Exit 0",
          "observed": "Exit 0 — compilation succeeded"
        }
      ],
      "evidence": {
        "unwrapCalls": "0 matches in asset_browser/mod.rs",
        "cargoCheck": "Exit 0"
      },
      "issues": null
    },
    {
      "id": "VAL-ROBUST-004",
      "title": "Guard TransformOptimization resource access",
      "status": "pass",
      "steps": [
        {
          "action": "rg 'contains_resource.*TransformOptimization' katla_app/",
          "expected": "ZERO matches",
          "observed": "0 matches — clean"
        },
        {
          "action": "cargo check -p katla_app",
          "expected": "Exit 0",
          "observed": "Exit 0 — compilation succeeded"
        }
      ],
      "evidence": {
        "containsResourceTransformOptimization": "0 matches",
        "cargoCheck": "Exit 0"
      },
      "issues": null
    },
    {
      "id": "VAL-ROBUST-005",
      "title": "Narrow World::storage_mut() exposure",
      "status": "pass",
      "steps": [
        {
          "action": "rg 'storage_mut' katla_ecs/src/world.rs",
          "expected": "Method should NOT exist (removed)",
          "observed": "0 matches — method removed"
        },
        {
          "action": "rg 'storage_mut' katla_app/",
          "expected": "ZERO callers",
          "observed": "0 matches — no callers"
        },
        {
          "action": "cargo check --workspace",
          "expected": "Exit 0",
          "observed": "Exit 0 — full workspace compiles"
        }
      ],
      "evidence": {
        "storageMutInWorld": "0 matches — method removed from world.rs",
        "storageMutCallers": "0 matches in katla_app/",
        "cargoCheckWorkspace": "Exit 0"
      },
      "issues": null
    },
    {
      "id": "VAL-ROBUST-006",
      "title": "UiContext fields pub(crate) with accessors",
      "status": "pass",
      "steps": [
        {
          "action": "rg 'pub (input|style|fonts):' katla_ui/src/context/mod.rs",
          "expected": "pub(crate) visibility on fields",
          "observed": "3 matches: pub(crate) input: UiInputState (line 63), pub(crate) style: UiStyle (line 65), pub(crate) fonts: FontSystem (line 67)"
        },
        {
          "action": "Check accessor methods exist",
          "expected": "input(), input_mut(), style(), style_mut(), fonts(), fonts_mut() defined",
          "observed": "6 accessor methods found: input() at line 197, input_mut() at line 202, style() at line 207, style_mut() at line 212, fonts() at line 217, fonts_mut() at line 222"
        },
        {
          "action": "rg 'ui\\.input\\.' katla_app/src/",
          "expected": "ZERO direct field access from katla_app",
          "observed": "1 match at katla_app/src/input/viewport_input.rs:24 — inside a doc comment (/// ```ignore block), NOT production code"
        },
        {
          "action": "rg 'ui\\.style\\.' katla_app/src/",
          "expected": "ZERO direct field access",
          "observed": "0 matches — clean"
        },
        {
          "action": "rg 'ui\\.fonts\\.' katla_app/src/",
          "expected": "ZERO direct field access",
          "observed": "0 matches — clean"
        },
        {
          "action": "cargo check --workspace",
          "expected": "Exit 0",
          "observed": "Exit 0 — full workspace compiles"
        }
      ],
      "evidence": {
        "pubCrateFields": "pub(crate) input (line 63), pub(crate) style (line 65), pub(crate) fonts (line 67)",
        "accessorMethods": "input()/input_mut() (lines 197-202), style()/style_mut() (lines 207-212), fonts()/fonts_mut() (lines 217-222)",
        "directFieldAccess": "ui.input. has 1 match in katla_app/src/ — inside doc comment (/// ```ignore), not production code. ui.style. and ui.fonts. have 0 matches.",
        "cargoCheckWorkspace": "Exit 0"
      },
      "issues": null
    }
  ],
  "frictions": [],
  "blockers": [],
  "summary": "Tested 6 assertions: all 6 passed. All rg pattern checks confirm the robustness milestone changes are in place. cargo check passes for katla_gfx, katla_app, and the full workspace."
}
