// Shared outline rendering constants.

// Outline color used by both the outline draw pass and the wallhack overlay in tonemapping.
const OUTLINE_COLOR: vec3f = vec3f(1.0, 0.55, 0.0);

// Wallhack overlay alpha for occluded selected objects (tonemapping shader only).
const WALLHACK_ALPHA: f32 = 0.4;
